# Progress

Rewritten in full after every scored session.

The matrix is the whole state. Rows are the scored dimensions from `RUBRIC.md`
plus `Overall`; columns are the difficulty rungs. Everything a report needs is
either derived from it or lives in `WEAKNESSES.md`.

Cell format is `avg (n)` — mean score across the `n` sessions where that
dimension was actually scored at that rung.

`null` = never demonstrated at that rung. `0` = attempted and failed.
Never average across rungs — a 7 at `easy` and a 7 at `hard` are different
achievements and stay in different columns.

**Sessions completed:** 0
**Last 5 overall:** none yet

## Proficiency matrix

| Dimension | basic | easy | medium | hard | architect |
|---|---|---|---|---|---|
| Overall | null | null | null | null | null |
| Requirements & scoping | null | null | null | null | null |
| Capacity & back-of-envelope math | null | null | null | null | null |
| API & data model | null | null | null | null | null |
| Core architecture & component choice | null | null | null | null | null |
| Scale, reliability & bottlenecks | null | null | null | null | null |
| Tradeoff reasoning & communication | null | null | null | null | null |
